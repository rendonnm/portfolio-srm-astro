---
import { gitHubColors } from "@/lib/constants/gitHub";
import GitHubDay from "./GitHubDay.astro";
import type { GitHubColors } from "@/lib/types/gitHub";

const levels = Object.keys(gitHubColors) as GitHubColors[];
---

<footer class="flex items-center gap-1 justify-end">
  <p class="text-xs text-zinc-400">Menos</p>
  {
    levels.map((level) => {
      const bgColor = gitHubColors[level];
      return <div class={`size-[10px] ${bgColor} rounded-xs`} />;
    })
  }
  <p class="text-xs text-zinc-400">Más</p>
</footer>
